---
title: Wyren
description: Shared brain for teams using Claude Code. One memory, every session warm.
template: splash
hero:
  tagline: Multiple humans. One brain. Zero workflow change.
  actions:
    - text: The problem
      link: /problem/
      icon: right-arrow
      variant: primary
    - text: How it works
      link: /how-it-works/
      icon: rocket
    - text: Source
      link: https://github.com/ssm-08/wyren
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## What is Wyren?

> **Git made code collaboration possible. Wyren does the same for the AI working alongside your team.**

Wyren is a Claude Code plugin that gives a team **one shared memory** across every teammate's sessions. No one has to change how they work.

Right now, every teammate has a different Claude — blank, unconfigured, and unaware of what the rest of the team has built, decided, or deliberately left unfinished. Wyren gives your whole team one shared brain.

When anyone on the team runs Claude Code, Wyren:

1. **Watches** the session transcript in the background.
2. **Distills** decisions, rejected paths, and live workarounds into a compact `memory.md`.
3. **Syncs** that memory to every teammate via git.
4. **Injects** it as hidden context at every new `SessionStart` — so any Claude, on any laptop, starts warm.

## The 10-second pitch

<CardGrid stagger>
	<Card title="The pain" icon="warning">
		The bottleneck isn't skill — it's context. Every new Claude session starts blank. Decisions, rejected paths, and workarounds from the last session vaporize when it closes.
	</Card>
	<Card title="The insight" icon="star">
		Markdown captures conclusions. Wyren captures the **reasoning** — the rejected ideas, live hacks, and deliberate shortcuts that look like bugs.
	</Card>
	<Card title="The result" icon="approve-check">
		Open Claude Code on a 4-hour-old project. Without Wyren: clueless. With Wyren: names the known workarounds and rejected approaches in the first assistant message, unprompted.
	</Card>
	<Card title="The footprint" icon="seti:config">
		One Claude Code plugin. Zero infra. Works with your existing git remote.
	</Card>
</CardGrid>

## How Wyren is different

| | What it does for teams |
|---|---|
| Git | Syncs code, not comprehension. |
| `CLAUDE.md` alone | Static; someone has to remember to write it — at the moment they have least time. |
| Claude Projects | Shared docs, but no live session memory or cross-teammate sync. |
| Agent Teams | Multi-agent coordination under one human. Doesn't help N humans with N Claudes. |
| **Wyren** | **Continuously distilled, git-synced, auto-injected team memory.** |

See [the problem](/problem/) for the full comparison matrix.

## Key selling points

<CardGrid>
	<Card title="Zero friction" icon="rocket">
		Nobody changes how they work. Open Claude Code, get full team context. No commands, no syncing, no briefing.
	</Card>
	<Card title="Works on handoff" icon="random">
		When work changes hands, the new session already knows everything. Zero briefing, zero dead zone, immediate productivity.
	</Card>
	<Card title="Captures the why" icon="information">
		The only tool that preserves the reasoning behind the codebase, not just the output of it.
	</Card>
	<Card title="Consistent behavior" icon="puzzle">
		Shared skills and settings mean every teammate's Claude behaves the same way on the same project.
	</Card>
</CardGrid>

## Get started

Start with [the problem](/problem/), walk through [how it works](/how-it-works/), then follow the [roadmap](/roadmap/overview/) for implementation details.
